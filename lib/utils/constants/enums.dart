// List Of Enums  They cannot be created inside class
enum ProductType { single, variable }

enum TextSizes {
  small,
  medium,
  large,
}

enum OrderStatus {
  processing,
  shipped,
  delivered
} // pending, accepted, rejected, delivered,

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  creditCard,
  debitCard,
  netBanking,
  cashOnDelivery,
}
